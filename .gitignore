# === Python ==================================================================
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/

# === Environment / secrets ===================================================
# Never commit real credentials. backend/.env.example is the tracked template.
.env
.env.*
!.env.example
backend/.env
# Firebase Admin service account key (backend token verification)
serviceAccountKey.json
*serviceAccount*.json
# Local test-account credential reference (password = email)
client_accounts.txt

# === Databases (local SQLite, never commit) ==================================
*.db
*.db-journal
*.db-wal
*.db-shm

# === Runtime data & generated artifacts ======================================
# Uploaded timesheets, Gmail attachments, voice audio, dedup store
storage/
# Generated invoice PDFs/HTML
backend/invoices_output/
# Generated analysis/output artifacts
analysis/

# === Local sample documents (keep repo lean; *.png allowed for README) =======
*.pdf
*.jpg
*.jpeg
*.webp
*.bmp
*.tif
*.tiff
*.xlsx
*.xls
*.xlsm
*.csv
docs/

# === Logs & OS junk ===========================================================
*.log
.DS_Store
Thumbs.db

# === Tooling ==================================================================
.claude/
.gstack/
